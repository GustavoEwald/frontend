function multiplicar(num1: number, num2: number): number {
  return num1*num2;
}

function saudacao(nome: string): string {
  return `Olá ${nome}`;
}

// testes
// console.log(multiplicar(29, 3));
// console.log(saudacao('Pedro'));